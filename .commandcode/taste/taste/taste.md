# Taste
- Communicates in Spanish and expects the assistant to respond in Spanish. Confidence: 0.9
- Uses the Spanish-language UI of external platforms (e.g., Meta Business Suite) and expects navigation guidance using the localized Spanish option names (e.g., "Activos de la empresa" for "Business assets", "Cuentas de Instagram"), not the English labels. Confidence: 0.6
- Communicates tersely and informally, often with typos (e.g., "sihas los fixes"), and expects the assistant to infer intent rather than ask for clarification. Fresh evidence: pasted a raw Graph API `/me/accounts` JSON response with no commentary at all and expected the assistant to interpret it and decide the next step; later messages were equally terse (e.g., "ya lo asigne otra vez verifica con este: <token>"). Confidence: 0.6
- Organizes development in phases (e.g., "Fase 1") and expects the assistant to verify the current status against the documented plan before proceeding — comparing planned vs. implemented work and surfacing pending items. Confidence: 0.6
- Expects code reviews/audits done with a senior software architect mindset — explicitly evaluating scalability and deliverability (production-ready, well-architected), not just correctness. Confidence: 0.7
- When the assistant surfaces non-blocking findings after an audit, he wants the proposed fixes actually applied (approves directly, e.g., "si, haz los fixes"), not just documented. Confidence: 0.5
- Manages the deployment target himself: the project lives on his remote server and he pulls the commits from there; the assistant should not worry about pushing or deploying to that remote. Confidence: 0.7
- Works on Windows with cmd as the shell — shell commands must not rely on POSIX utilities like `tail` (a `2>&1 | tail -30` pipe failed and had to be re-run without it). Confidence: 0.4
- Likes practical handoff docs for putting features into production: a checklist of the exact values/credentials needed, where each one goes (.env vs. encrypted DB seed), how to capture them, and troubleshooting. Confidence: 0.5
- Wants every code change covered by tests (unit tests added alongside fixes, e.g., a parser case for `is_self` and a timeout test for the adapter), and expects the full check suite to stay green after changes. Confidence: 0.6
- Delegates external API data-gathering to the assistant when feasible: he will hand over access tokens and ask the assistant to make the API calls directly (e.g., Graph API calls to verify IDs) rather than doing them manually in the Graph API Explorer. He reserves manual steps for UI-only actions only he can perform (e.g., linking the Instagram account in Meta Business Suite). When he does run calls himself, he pastes the raw output back with no commentary for the assistant to interpret. He also pastes whatever credentials he has (e.g., an `IGAA...` IG token) without knowing which type the integration needs, and expects the assistant to test them, identify which are usable vs. not, and explain why. Fresh evidence: asked "no habra alguna forma que tu hagas las llamadas y consigas los datos?", and the assistant's direct Graph API calls confirmed the IGID; later pasted an IG Access Token ("te sirve este token: IGAA...") which the assistant validated via curl and classified as unusable for the Messenger Platform. He also explicitly parallelizes the work: told the assistant "ve arreglando lo que necesitas, yo busco como hacer ese vinculo de la pagina" — i.e., the assistant should proceed autonomously fixing code/config (e.g., editing the `.env` with feature flags and tokens) without waiting for his input, while he personally handles the manual UI-only step (linking IG in Meta Business Suite). After completing his manual step, he comes back and asks the assistant to re-run the verification/API calls himself ("creo que ya se conectadorn las cuentas porfa vuelve a intentar obtener el token") — he expects the assistant to actively re-test credentials and links after external changes instead of trusting his own report. Confidence: 0.95
- Prefers permanent, non-expiring credentials for the integration over short-lived dev tokens (handed over a Meta "permanent" token with the instruction "usa este token que es permanente" so re-verification/re-generation isn't needed), and expects the assistant to verify that a provided token actually has the required asset assignments and scopes (e.g., a System User token that lacks the Page as an assigned asset fails on Page-level reads/webhook subscription) and to surface the misconfiguration rather than silently wiring it in. Fresh evidence: after he fixed the assignment and provided a regenerated token, the assistant verified it reads Page + IG link + IGID before updating the `.env`, and he accepted this workflow without further instruction. Confidence: 0.7
- Prefers consolidating redundant scripts into a single unified entry point rather than maintaining several scattered versions — explicitly asked to unify all the seeds ("trata de unificar todos los seeds") into one `seed.ts` covering business + channel accounts + catalog + users, replacing the separate SQL seeds (which used destructive TRUNCATE and plaintext token placeholders) with one idempotent script that reads real credentials from `.env` and encrypts them. Confidence: 0.6
- Prefers to run database/seed commands himself rather than have the assistant execute them: when a new seed/script lands, he asks for the exact command to run (e.g., "dame el comando para hacer correr el seed nuevo") and expects it accompanied by the required `.env` prerequisites and a note on idempotency. He runs these commands on the remote Linux (Ubuntu) server himself, and when they fail there he pastes the raw error output and expects the assistant to diagnose the root cause and hand back the exact fix command. Fresh evidence: `npm run prisma:seed` failed on the remote with `Cannot find module 'bcrypt'` and he pasted the full stack trace. Confidence: 0.7

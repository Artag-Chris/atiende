# Taste — Code & Tooling
- When adding a new AI provider (LLM/chat model), it must mirror the existing provider-adapter pattern rather than be built ad hoc — he asks to check whether the provider is already wired "segun los patrones que tenemos para responder con IA" and, if not, to create it "segun los otros patrones de distintas IA". The expected work follows the established structure: a new adapter + DI module in `src/modules/llm/{provider}/`, a config/pricing block (incl. `cacheReadPer1M` etc.), env schema with per-provider vars (`{PROVIDER}_API_KEY`/`_MODEL`/`_MAX_TOKENS`/`_TIMEOUT_MS`/`_MAX_RETRIES`) + validation that requires the key when that provider is primary/fallback/analytics, wiring into `features.ts`, `module-registry.ts`, `llm-router.module.ts`, `analytics-llm.module.ts`, updates to both `.env` and `.env.example`, and a co-located spec following an existing provider's spec pattern. Fresh evidence: asked to add DeepSeek and wire it end-to-end as the primary provider, which was implemented in exactly this way and validated with typecheck + full test suite. Confidence: 0.8
- Selects AI providers/models with cost as the deciding factor, favoring fast+cheap options over more expensive ones — explicitly chose DeepSeek "por costo" and asked for "el ultimo flash de deepseek que es muy rapido y bueno"; expects the assistant to pick the economical "flash"-style model identifier and register current official pricing for it (input/output per million tokens with cache rates modeled after the OpenAI pattern) rather than the higher-tier reasoning models. Confidence: 0.85